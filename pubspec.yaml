name: flutter_debug_trinity
description: >
  Unified crash recovery (recoverable_app), explainable UI decisions (ui_explainer),
  and root-cause causality tracing (causality_flutter) — all sharing one event graph.
version: 0.1.0
homepage: https://github.com/chirag640/flutter_debug_trinity
repository: https://github.com/chirag640/flutter_debug_trinity
issue_tracker: https://github.com/chirag640/flutter_debug_trinity/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  uuid: ^4.3.3
  shared_preferences: ^2.2.3
  crypto: ^3.0.3
  collection: ^1.18.0
  provider: ^6.1.2
  flutter_bloc: ^8.1.6
  flutter_riverpod: ^2.5.1
  dio: ^5.4.3
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  mocktail: ^1.0.3

# Dart pub score optimizations:
topics:
  - debugging
  - devtools
  - state-management
  - crash-recovery
  - flutter
